<form method="post" action="{{ path('app_order_product_delete', {'salesOrder': order_product.salesOrder}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order_product.salesOrder) }}">
    <button class="btn">Delete</button>
</form>
