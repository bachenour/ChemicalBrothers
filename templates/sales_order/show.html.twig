{% extends 'base.html.twig' %}

{% block title %}SalesOrder{% endblock %}

{% block body %}
    <h1>SalesOrder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sales_order.id }}</td>
            </tr>
            <tr>
                <th>DateOrder</th>
                <td>{{ sales_order.dateOrder ? sales_order.dateOrder|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DateDelivery</th>
                <td>{{ sales_order.dateDelivery ? sales_order.dateDelivery|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sales_order_index') }}">back to list</a>

    <a href="{{ path('app_sales_order_edit', {'id': sales_order.id}) }}">edit</a>

    {{ include('sales_order/_delete_form.html.twig') }}
{% endblock %}
